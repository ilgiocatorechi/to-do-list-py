# Terminal To-Do List (Python)

A simple Python program to manage a to-do list from the terminal. Data is saved in a local file called `tasks.txt`.

## How to Use

1. Run `todo.py` with Python 3
2. Follow the menu to add, remove, or view tasks
3. Tasks are automatically saved when exiting

## Requirements

- Python 3 installed
